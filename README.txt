
Written in C.

Loadable kernel module and kernel patch that allows users to tag a process and print out all processes tagged.

tagkill is a shell script that will kill 9 all tagged processes 

The Kernel patch adds a system call that allows processes to be tagged.

